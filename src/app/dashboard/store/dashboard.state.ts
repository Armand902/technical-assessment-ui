import { TargetAsset } from "./target-asset";

//move inital state here for seperation of concern.

export const initialState: ReadonlyArray<TargetAsset> = [];

export const initialLoadingState: boolean = true;
